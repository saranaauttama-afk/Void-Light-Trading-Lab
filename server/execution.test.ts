import{describe,it,expect}from'vitest';import{LiveExecutionEngine,PaperExecutionEngine}from'./execution';import{createOrderIntent}from'./order-intent';import type{LabState}from'../shared/types';const state=():LabState=>({mode:'PAPER',status:'RUNNING',cash:100,asset:0,equity:100,dailyPnl:0,dailyPnlPct:0,trades:[],risk:{startingBalance:100,dailyProfitTargetPct:2,dailyLossLimitPct:2,maxTradesPerDay:6,maxPositionPct:25,cooldownMinutes:15}});describe('execution boundary',()=>{it('paper engine executes against simulated state',async()=>{const s=state(),r=await new PaperExecutionEngine().execute(createOrderIntent('BTCUSDT','BUY',10,'test'),100,s);expect(r.ok).toBe(true);expect(s.cash).toBe(90);expect(s.asset).toBe(.1);expect(s.trades).toHaveLength(1)});it('live boundary never creates an order',async()=>{const r=await new LiveExecutionEngine().execute(createOrderIntent('BTCUSDT','BUY',10,'test'),100,state());expect(r.ok).toBe(false);expect(r.message).toContain('not installed')})});