PORT=8787
VITE_API_BASE=http://localhost:8787
# Server-side only. Never put this in a VITE_* variable or browser code.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
# Live exchange credentials and live execution are intentionally unsupported.
